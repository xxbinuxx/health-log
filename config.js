// Paste the two values from Supabase here (SETUP.md, step 4).
// Project URL looks like  https://abcdefghijklmnop.supabase.co
// The "anon public" key is a long string starting with "eyJ". It is safe to keep in this file:
// the database only lets each signed-in person read and write their own rows.

export const SUPABASE_URL = "PASTE_YOUR_PROJECT_URL_HERE";
export const SUPABASE_ANON_KEY = "PASTE_YOUR_ANON_KEY_HERE";
